# Copy this file to `deploy.env` (gitignored) and fill in the real values.
# See CLAUDE.md "Deployment" for context.

export DOCS_DEPLOY_HOST=root@<droplet-ip>
export DOCS_DEPLOY_PATH=/var/www/docs.reddcoin.com/
export DOCS_DEPLOY_KEY=$HOME/certs/<your-ssh-key>.pem
